apiVersion: v1
kind: Service
metadata:
  name: redis-svc
  labels:
    app: redis
    tier: database
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: redis-db
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
    protocol: TCP
