version: '3.8'
services:
  redis:
    image: "redis:alpine"
    restart: always
    ports:
      - "6379:6379"
    
  api:
    build: ./app
    container_name: backend_api
    ports:
      - "8080:8080"
    depends_on:
      - redis