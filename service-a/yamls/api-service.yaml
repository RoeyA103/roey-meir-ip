apiVersion: v1
kind: Service
metadata:
  name: coordinator-api-svc

spec:
  type: NodePort
  selector:
    app: coordinator-api-1
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30050
    protocol: TCP


